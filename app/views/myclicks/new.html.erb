

<% if !@myclick.errors.empty? %>
   <div class = "alert alert-error">
      <ul>
         <% @myclick.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
         <% end %>
      </ul>
   </div>
<% end %>

<div class = "well">
   <%= form_for @myclick, html: { multipart: true } do |f| %>
      <%= f.label :name %>
      <%= f.text_field :name %>
      <%= f.label :attachment %>
      <%= f.file_field :attachment %>
      <%= f.label :type %>
      <%= f.text_field :type %>
      <%= f.submit "Save", class: "btn btn-primary" %>
   <% end %>
</div>